##################
Create Connection
##################

The full connection wizard (direction, template/expert mode, method designer,
operators, webhook parameters, aggregators, testing, etc.) lives in the usage
manual.  Administrators use the exact same UI as regular project owners, so
there is no separate “management” flow to describe here.  Refer to
:doc:`../usage/connections` for the step-by-step guide and keep that article as
the single source of truth.