import {ITheme} from "@style/Theme";
import {PermissionProps} from "@application/interfaces/IApplication";

interface SupportFilesListProps{
    theme?: ITheme,
    permission?: PermissionProps;
}

export {
    SupportFilesListProps,
}
