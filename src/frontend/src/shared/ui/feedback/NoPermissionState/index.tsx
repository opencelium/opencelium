import {useI18n} from "@shared/i18n/hooks/useI18n.ts";

export function NoPermissionState({ }) {
    const { t } = useI18n('')

    return (
        <>
            No Permission State
        </>
    )
}