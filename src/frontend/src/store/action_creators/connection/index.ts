import ConnectionCreators from "@action/connection/ConnectionCreators";
import TemplateCreators from "@action/connection/TemplateCreators";

export default {
    ...ConnectionCreators,
    ...TemplateCreators,
}